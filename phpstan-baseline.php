<?php declare(strict_types = 1);

// PHPStan baseline — locks in existing errors so new code cannot add more.
// Regenerate with: vendor/bin/phpstan analyse --generate-baseline=phpstan-baseline.php
// This empty baseline means: no existing errors are ignored. All new errors
// must be fixed before merge. As the codebase matures, regenerate this file
// to capture the current state and prevent regression.

return [];
